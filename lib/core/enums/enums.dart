// ignore_for_file: constant_identifier_names

enum Gender { male, female }

enum ResendOtpType { verify_email, reset_password }

enum SuccessType { verifyPhone, resetPassword, changePassword, createOrder }

enum PaymentMethods { cash, online }

enum OtpMethodType { sms, whatsapp }

enum VerifictionType { verifyPhone, reset_password }
